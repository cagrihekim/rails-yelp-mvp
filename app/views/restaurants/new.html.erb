<div class="form">
  <%= simple_form_for(@restaurant) do |f| %>
    <%= f.input :name %>
    <%= f.input :category %>
    <%= f.input :phone_number %>
    <%= f.submit 'Add Restaurant', class: 'button' %>
  <% end %>
</div>
